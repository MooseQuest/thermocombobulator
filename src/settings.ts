/** The platform name registered with Homebridge (must match config.schema.json "pluginAlias"). */
export const PLATFORM_NAME = 'Thermocombobulator';

/** The npm package name (must match package.json "name"). */
export const PLUGIN_NAME = 'homebridge-thermocombobulator';
